extends layout

block content
    .card
        .card-body
            .card
                .card-body
                    a.btn.btn-outline-success.mr-5(href="/administrar") Regresar
            .card
                .card-body
                    .container-fluid.col-sm-8#table
                        h3#text LISTA DE PLATOS
                        hr
                        br
                        table.table.bg-white
                            thead.text-center
                                tr(style="text-align:center;")
                                th(scope='col') Id
                                th(scope='col') Descripción
                                th(scope='col') Tipo de plato
                                th
                            tbody
                            -for(let i = 0; i <dish.length; i++)
                                tr(style="text-align:center;")
                                    th(scope='row')
                                        h5 #{dish[i].id}
                                    td
                                        h5 #{dish[i].description}
                                    td
                                        h5 #{dish[i].Dish_type_id}
                                    td
                                        form(method='POST', action='/deleteDish')
                                            button.btn.btn-danger(value=dish[i].id, name='id') X
                    div.text-center
                        a.btn.btn-success.d-inline.m-3(href='/create-dish')  Agregar plato
