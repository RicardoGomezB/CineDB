extends layout

block content
    .card
        .card-body
            .card
                .card-body
                    a.btn.btn-outline-success.mr-5(href="/administrar") Regresar
            .card
                .card-body
                    .container-fluid.col-sm-8#table
                        h3#text TIPOS DE PLATO
                        hr
                        br
                        table.table.bg-white
                            thead.text-center
                                tr(style="text-align:center;")
                                th(scope='col') Id
                                th(scope='col') Descripción
                                th
                            tbody
                            -for(let i = 0; i <dishType.length; i++)
                                tr(style="text-align:center;")
                                    th(scope='row')
                                        h5 #{dishType[i].id}
                                    td
                                        h5 #{dishType[i].Description}
                                    td
                                        form(method='POST', action='/deleteDishType')
                                            button.btn.btn-danger(value=dishType[i].id, name='id') X
                    div.text-center
                        a.btn.btn-success.d-inline.m-3(href='/create-dish-type')  Agregar plato
