extends layout

block content
    .col-sm-9#form
        h3 ELIMINAR PELÍCULA
        hr
        br
        .card
            .card-body  
                form(method="POST", action="/delete-pelicula" )
                    .row
                        .col-sm-3
                            select(name='TITLE', class="form-control", id="TITLE")
                                -for(let i = 0; i < movie.length; i++)
                                    option(value=movie[i].TITLE) #{movie[i].TITLE}
                    br
                    button.btn.btn-primary.btn-lg(type="submit", style="position:relative; left:45%;" ) Eliminar