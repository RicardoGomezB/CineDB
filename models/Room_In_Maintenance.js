const Sequelize = require("sequelize");
Sequelize.Promise = global.Promise;
const sequelize = require("../config/database");

const User = sequelize.define("Room_In_Maintenance", {
    //ROOM_ID
  });
  
  module.exports = Room_In_Maintenance;